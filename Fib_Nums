import sys
fib = [1, 1]
def fibonacci():
    print("How many fibonacci numbers do you want?")
    nums = int(sys.stdin.readline())
    int(nums)
    for x in range(0, nums):
        sum = fib[x] + fib[x+1]
        fib.append(sum)
    print(fib)

fibonacci()